# AI Code Review Kit 设计文档

## 设计概述

### 目标

1. 用同一条命令在 macOS、Linux 和 Windows 上发起 AI 代码审查；
2. 准确覆盖工作区、暂存区、指定提交和基线差异；
3. 在代码离开本机前执行敏感文件过滤、内容脱敏和大小限制；
4. 允许团队版本化提示词与排除规则；
5. 安装和运行都不要求管理员权限或第三方 Python 包。
6. 在 commit/push 前执行失败关闭的 AI 门禁，并对每条风险进行人工确认。

### 非目标

- 不自动修改代码，也不代替用户发起提交或推送；
- 不替代 CI、人工审批、SAST 或密钥扫描；
- 不内置云厂商 API 密钥管理；
- 不自动判断风险“可以忽略”，风险处置必须由操作者逐条确认；
- 不保证在所有自定义 AI CLI 上实现沙箱隔离。

## 架构设计

### 整体架构

```text
GitHub online bootstrap（可选）
          │ HTTPS 下载固定仓库/ref
          ▼
install.sh / install.ps1
          │
          ▼
   用户级 ai-review 命令
          │
          ├──────────────► Git hooks 安装/备份/恢复
          │                       │
          │                pre-commit / pre-push
          ▼                       ▼
 Git 范围解析 ──► 路径过滤 ──► diff 采集 ──► 内容脱敏/截断
                                                │
                                                ▼
                                        审查提示模板渲染
                                                │
                              ┌─────────────────┼─────────────────┐
                              ▼                 ▼                 ▼
                       Codex 只读执行    自定义参数数组命令      手动输出提示
                              │                 │
                              └────────┬────────┘
                                       ▼
                              结构化 JSON 校验
                                       ▼
                         逐条处置 + 最终 CONFIRM
                                       ▼
                              允许或阻断 Git 操作
```

### 核心组件

- **在线引导层**：`install-online.sh` 和 `install-online.ps1` 从固定 GitHub 仓库/ref 下载最小载荷，校验 ref 与文件非空后调用本地安装器；
- **安装层**：`install.sh` 负责 macOS/Linux，`install.cmd`/`install.ps1` 负责 Windows；将脚本和数据复制到用户目录，并创建平台入口；
- **Git 采集层**：只通过参数数组调用 Git，手动模式支持 `working`、`staged`、`commit`、`base`；`pre-push` 解析 Git 标准输入中的 ref 更新并审查精确前后对象；
- **过滤层**：按默认规则和项目配置排除文件，对未跟踪文件执行路径、符号链接、大小、二进制与 UTF-8 检查；
- **脱敏层**：在最终 diff 上替换常见密钥赋值、令牌和私钥块，并施加最大字符数；
- **提示层**：从版本化 Markdown 模板渲染仓库、范围、采集元数据和不可信 diff；
- **执行层**：内置 Codex 只读适配器，并允许配置任意标准输入型 AI CLI；手动模式无可用执行器时输出提示，门禁模式则失败关闭；
- **门禁层**：为模型附加固定 JSON 协议，严格验证严重级别和风险字段；截断/跳过也转换成覆盖风险；
- **交互层**：每条风险必须选择接受、误报或停止，全部通过后仍需输入 `CONFIRM`；`pre-push` 从控制终端读取选择，避免与 ref 标准输入冲突；
- **Hook 生命周期层**：安装前备份已有 hook，生成串联包装器，重复安装只更新包装器，卸载时恢复原 hook。

## 设计决策

### 方案选择

评估过三种实现：

| 方案 | 优点 | 缺点 | 结论 |
|---|---|---|---|
| 仅 Bash + PowerShell 各写一套 | 无 Python 依赖 | 核心逻辑重复，平台行为容易漂移 | 不采用 |
| Node.js 单一 CLI | JSON 和跨平台体验较好 | 需要额外 Node/npm 安装链 | 不采用 |
| Python 标准库核心 + 双平台安装器 | 一套审查逻辑、依赖常见、可测试 | 需要 Python 3.9+ | 采用 |

### 关键决策

| 日期 | 决策 | 理由 | 影响 |
|---|---|---|---|
| 2026-09-01 | 核心只使用 Python 标准库 | 降低安装复杂度和供应链风险 | 最低版本为 Python 3.9 |
| 2026-09-01 | 自定义命令使用 JSON 参数数组 | 避免字符串拼接与 shell 注入 | AI CLI 必须能从标准输入读取提示 |
| 2026-09-01 | Codex 使用 `exec --ephemeral --sandbox read-only` | 非交互、无持久会话且禁止写仓库 | 仍继承用户的 Codex 认证与网络策略 |
| 2026-09-01 | 在生成提示前过滤路径和内容 | 减少敏感信息进入模型上下文的概率 | 极少数误报需要显式绕过或调整规则 |
| 2026-09-01 | 未发现 AI CLI 时输出提示 | 保证工具在所有平台仍可使用 | 用户需手动粘贴给 AI 服务 |
| 2026-09-01 | 不自动编辑 shell profile | 避免安装器意外破坏用户配置 | POSIX 用户可能需手动加入 PATH |
| 2026-09-01 | 项目配置必须显式信任 | 防止不可信仓库通过配置指定本地命令 | 使用时增加 `--trust-project-config` |
| 2026-09-01 | 在线安装采用下载后委托本地安装器 | 避免复制两套安装逻辑并便于测试 | 在线安装依赖 GitHub 可用性与仓库可读性 |
| 2026-09-01 | ref 仅允许简单分支或标签字符 | 防止 URL/路径注入 | 不支持名称中带 `/` 的 ref |
| 2026-09-01 | commit 审查暂存区，push 审查 Git 提供的 ref 范围 | 与最终提交/推送内容一致，减少工作区噪音 | 同一变更可能在 commit 和 push 各审查一次 |
| 2026-09-01 | 门禁只接受单个结构化 JSON 对象 | 格式可验证，避免从自由文本猜测风险 | 不符合协议的模型或执行器会阻断 Git |
| 2026-09-01 | 所有风险逐条处置后再输入 `CONFIRM` | 防止一次确认掩盖多条风险或误按回车 | 有风险时必须有交互终端 |
| 2026-09-01 | hooks 默认失败关闭 | AI 不可用时不能把“未审查”误当成“无风险” | 离线提交需恢复 AI 或使用受控例外流程 |
| 2026-09-01 | 自动安装拒绝 `core.hooksPath` | 避免修改可能被多个仓库共享的 hook 目录 | 使用自定义 hooksPath 的团队需手动集成 |

### 技术选型

- **Python 3.9+**：跨平台核心逻辑、JSON、子进程、路径与测试均由标准库提供；
- **POSIX sh**：兼容 macOS 和主流 Linux 默认 shell；
- **PowerShell 5.1+**：Windows 用户级安装和可选 PATH 更新；
- **JSON**：避免 TOML 在 Python 3.9/3.10 上需要第三方依赖；
- **Markdown**：提示模板和文档可以直接评审与版本控制。

## 权衡取舍

### 已知限制

- Windows PowerShell 安装脚本无法在非 Windows 且未安装 `pwsh` 的环境中动态执行，只能静态校验；
- 基于文件名和正则的脱敏是尽力而为，不能替代专用密钥扫描；
- 默认只读取 UTF-8 未跟踪文件，其他编码文件会跳过；
- diff 按字符数截断，不做模型专属 token 估算；
- 自定义 AI CLI 的认证、联网、数据保留和沙箱策略由该 CLI 自身负责；
- Git 子模块只显示 Git 记录的子模块引用变化，不递归审查子模块工作区。
- 匿名在线安装要求 GitHub 仓库公开可读，私有仓库必须使用已认证的克隆方式；
- 在线安装通过 HTTPS 保护传输，但 `main` 是可变引用；高保证环境必须固定到经过审查的发布标签或提交包。
- 新分支首次推送会从最近的同名远程边界审查；找不到边界时从空树审查，较大仓库可能触发长度上限；
- 没有控制终端的 IDE 可在无风险时继续，但发现风险后无法完成逐条确认，会默认阻断；
- Git 原生 `--no-verify` 能绕过客户端 hook，组织级强制策略仍需在受保护分支 CI/服务端执行。

### 技术债务

核心目前保持为单文件 CLI，以便在线安装器只分发一个 Python 运行载荷；随着门禁能力加入，该文件已接近 1000 行，质量扫描会给出文件长度警告。后续拆分时需要同步调整 POSIX/Windows/在线安装载荷，因此把模块化列为非阻断技术债务。其他可选演进包括 AI 审查结果缓存、CI 注释器、服务端策略和原生包管理器发布。

## 安全考量

### 威胁模型

1. **命令注入**：恶意仓库路径、文件名、Git 引用或自定义参数诱导 shell 执行；
2. **路径遍历与符号链接**：未跟踪文件指向仓库外部敏感数据；
3. **敏感信息泄漏**：diff 包含 `.env`、私钥、令牌、密码或大块内部数据；
4. **提示注入**：源代码注释要求 AI 忽略审查任务、执行命令或泄露环境信息；
5. **AI 写入工作区**：代码审查代理意外修改被审查内容；
6. **安装器越权或覆盖**：安装脚本修改系统目录、管理员范围 PATH 或无关文件；
7. **不可信自定义执行器**：用户配置的命令读取提示后执行任意本地行为。
8. **在线安装供应链攻击**：仓库、分支或下载链路被篡改后向用户分发恶意载荷。
9. **模型输出欺骗**：提示注入诱导模型输出多段内容、伪造低风险结论或终端控制序列。
10. **Hook 覆盖与共享目录污染**：安装门禁时破坏已有 hook，或误改全局 `core.hooksPath`。
11. **私有仓库令牌泄漏**：在线安装认证值进入命令历史、进程参数、日志或长期文件。

### 安全边界

- Git 仓库、文件内容和文件名均视为不可信输入；
- 用户配置和显式 `--config` 视为操作者信任的本地配置，能指定外部命令；
- 仓库内 `.ai-review.json` 默认不加载，只有 `--trust-project-config` 才把它纳入信任边界；
- AI CLI、其认证状态、模型服务与网络传输位于本工具信任边界之外；
- 安装目录和用户级 `PATH` 属于当前操作系统用户的权限边界；
- GitHub 仓库和用户选择的 ref 构成在线安装的代码信任边界；
- `--allow-sensitive` 是显式的安全策略降级，调用者承担相应风险。

### 安全措施

- 所有 Git 和 AI CLI 调用均使用参数数组，不使用 `shell=True`、`eval` 或字符串解释；
- Git 引用拒绝前导 `-`、换行和 NUL，并在采集前验证为提交；
- 用户目标路径必须存在，并在使用前展开、规范化符号链接；
- 未跟踪文件解析为绝对路径后必须仍位于仓库内，且拒绝符号链接、二进制、超大或非 UTF-8 文件；
- 路径过滤、内容脱敏与大小限制都在 AI CLI 启动前完成；
- Codex 适配器固定为只读沙箱与临时会话；
- diff 放入显式不可信边界，提示禁止遵循代码中的指令；
- 安装器只写用户指定目录，Windows PATH 更新必须显式传入 `-AddToPath`；
- 在线安装器固定 GitHub 所有者/仓库，ref 使用白名单校验，只允许 HTTPS，并拒绝空载荷；
- 私有仓库使用 GitHub Contents API；POSIX 令牌只写入 `umask 077` 创建的临时 curl 配置，并由退出陷阱删除；PowerShell 通过请求头传递令牌；
- Windows 在线安装器落盘后执行，不使用 `Invoke-Expression`；生产使用可通过标签固定载荷版本；
- 项目配置需要显式信任，避免审查对象默认控制自定义执行命令；
- 门禁提示位于不可信 diff 之后，并只接受一个完整 JSON 对象；缺字段、非法严重级别、多余自由文本或命令错误都会阻断；
- 模型提供的风险字段在终端显示前清理控制字符，防止改变终端状态；
- 被过滤或截断的内容转化为显式覆盖风险，不能静默当作已审查；
- 只覆盖带固定管理标记的 hook；原 hook 先备份、串联运行并可恢复；检测到 `core.hooksPath` 时拒绝自动安装；
- 单元测试覆盖敏感过滤、脱敏、恶意引用、自定义命令、结构化结果、推送范围、逐条确认、hook 恢复与安装冒烟。

### 已接受风险

- 有些凭据不符合已知文件名或内容形态，仍可能进入提示；
- 用户主动配置的自定义命令拥有当前用户权限，本工具无法约束其行为；
- AI 服务的数据处理条款随服务商和账号策略变化，使用前必须由团队自行确认。
- 默认 `main` 在线安装无法提供不可变供应链保证，接受该风险的用户应先检查脚本；高保证环境必须固定发布标签。
- 客户端 Git hooks 可以被拥有本地仓库控制权的用户用 `--no-verify` 绕过，工具将该能力作为需审计的紧急出口而不是组织级安全边界。
- 环境变量中的 GitHub 令牌仍属于当前用户的进程安全边界，用户必须通过凭据管理器注入最小权限短期令牌并在安装后清除。

## 测试策略

- **单元测试**：Git 范围、敏感文件过滤、凭据脱敏、截断、配置、引用、JSON schema 与逐条确认；
- **集成测试**：临时 Git 仓库运行 CLI，验证标准输入型自定义执行器、pre-commit 和精确 pre-push ref 范围；
- **Hook 测试**：验证已有 hook 备份串联、幂等升级、执行门禁、卸载恢复和共享路径拒绝；
- **安装测试**：临时目录执行 POSIX 安装器并运行已安装命令；
- **在线安装测试**：使用本地受控下载替身验证载荷列表、ref 拒绝、路径含空格和安装后命令；
- **跨平台静态检查**：验证 Windows 资产完整且不含动态表达式执行；
- **发布门禁**：模块完整性、代码质量、安全与代码/文档同步扫描。

## 变更历史

### 2026-09-01 - 私有仓库在线安装

**变更内容**：在线安装器新增 `GITHUB_TOKEN`/`GH_TOKEN` 与 PowerShell `-Token` 支持，并补充私有仓库的 GitHub Contents API 引导命令。

**变更理由**：仓库当前不可匿名读取，公开 raw URL 会返回 404，必须支持最小 Contents: read 权限认证才能直接安装。

**影响范围**：两个在线安装器、认证路径测试、README 和在线安装威胁模型。

**决策依据**：令牌不持久化到安装目录；POSIX 使用权限受限的临时 curl 配置，避免把 Authorization 值放入 curl 命令参数。

### 2026-09-01 - Git commit/push AI 门禁

**变更内容**：新增 `pre-commit`/`pre-push` 安装和恢复、结构化 AI 风险协议、覆盖风险、逐条处置与最终确认。

**变更理由**：把手动代码审查升级为提交前自动执行、失败关闭且由人工完成风险决策的工作流。

**影响范围**：CLI 核心、Git hooks、AI 输出契约、测试、README 和安全模型。

**决策依据**：commit 只审查暂存区，push 以 Git 提供的 ref 更新为事实来源；已有 hook 可恢复，AI 和格式错误一律阻断。

### 2026-09-01 - GitHub 在线安装与配置文档

**变更内容**：新增 macOS/Linux 与 Windows GitHub 在线安装器、在线安装集成测试，并扩展 AI 登录、执行器配置、命令范围和升级文档。

**变更理由**：允许用户无需手动克隆仓库即可安装，同时让 AI 凭据和运行配置方式可直接照抄使用。

**影响范围**：安装入口、测试、README 和在线安装供应链威胁模型。

**决策依据**：复用本地安装器保持单一安装逻辑；固定仓库、校验 ref、HTTPS 下载和显式临时文件降低在线执行风险。

### 2026-09-01 - 初始跨平台版本

**变更内容**：创建 AI 代码审查 CLI、提示模板、默认配置、macOS/Linux 与 Windows 安装器、测试和文档。

**变更理由**：提供可重复、安全且无需管理员权限的跨平台 AI 代码审查入口。

**影响范围**：新增独立目录 `ai-code-review-kit`，不修改当前目录中的其他项目。

**决策依据**：以标准库和用户级安装降低环境要求、供应链风险与现有项目侵入性。
